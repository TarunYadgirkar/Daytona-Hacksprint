{
  "name": "safeship",
  "version": "0.1.0",
  "private": true,
  "description": "Adversarial PR verification gate. Extracts a PR's behavioral claim, generates tests designed to break it, and runs them in a sandbox for real pass/fail evidence.",
  "packageManager": "npm@10.8.2",
  "engines": {
    "node": ">=20.20.0 <21"
  },
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "typecheck": "tsc --noEmit",
    "record:coderabbit": "tsx --env-file=.env scripts/record-coderabbit.ts",
    "check:env": "tsx --env-file=.env scripts/check-env.ts",
    "smoke": "tsx --env-file=.env scripts/smoke.ts",
    "test": "tsx --test tests/*.test.ts"
  },
  "dependencies": {
    "@copilotkit/react-core": "latest",
    "@copilotkit/react-ui": "latest",
    "@copilotkit/runtime": "latest",
    "@daytona/sdk": "latest",
    "braintrust": "latest",
    "next": "^15.5.0",
    "openai": "^6.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
